% =============================================================================
% PAGE DE GARDE -- fond sarcelle pleine page, logo Rihen en reserve.
% Le logo est en couleurs sur fond blanc dans sa version d'origine ; ici c'est
% un BANDEAU BLANC qui le porte, plutot qu'une detourure approximative : on ne
% deforme pas une marque pour l'adapter a un fond.
% =============================================================================
\begin{titlepage}
\thispagestyle{empty}
\newgeometry{margin=0pt}

\begin{tikzpicture}[remember picture, overlay]
  % Fond
  \fill[rihenTeal] (current page.south west) rectangle (current page.north east);
  % Aplat orange en bas : la marque a deux couleurs, la page de garde aussi.
  \fill[rihenOrange] ([yshift=0pt]current page.south west)
        rectangle ([yshift=13pt]current page.south east);
  % Bandeau blanc portant le logo, ancre en haut.
  \fill[white] ([yshift=-2.6cm]current page.north west)
        rectangle ([yshift=-6.4cm]current page.north east);
  \node[anchor=center] at ([yshift=-4.5cm]current page.north)
       {\includegraphics[width=8.2cm]{rihen-logo.png}};

  % Titre
  \node[anchor=north, text width=15.2cm, align=center]
       at ([yshift=-9.4cm]current page.north) {
    {\sffamily\bfseries\fontsize{34}{40}\selectfont\color{white}
      Construire avec Nkentseu\par}
    \vspace{10pt}
    {\color{rihenOrange}\rule{4.2cm}{2.6pt}\par}
    \vspace{16pt}
    {\sffamily\fontsize{17}{23}\selectfont\color{white!88}
      De la première ligne de C\\à dix applications complètes\par}
    \vspace{10pt}
    {\sffamily\fontsize{12.5}{17}\selectfont\color{rihenOrange}
      Cours complet — débutant à professionnel\par}
    % L'auteur porte sur la couverture, pas seulement dans les credits : c'est
    % lui qui detient les droits sur cet ouvrage.
    \vspace{26pt}
    {\sffamily\fontsize{13.5}{19}\selectfont\color{white}
      \DocAuteur\par}
    \vspace{5pt}
    {\sffamily\fontsize{10}{14}\selectfont\color{white!70}
      \DocAuteurTitres\par}
  };

  % Bas de page
  \node[anchor=south, text width=15cm, align=center]
       at ([yshift=1.9cm]current page.south) {
    {\sffamily\fontsize{10.5}{15}\selectfont\color{white!78}
      Rêvons ensemble et réinventons le futur\par}
    \vspace{5pt}
    {\sffamily\fontsize{9.5}{13}\selectfont\color{white!58}
      Rihen — \DocDate\par}
  };
\end{tikzpicture}
\end{titlepage}
% ⚠️ `\restoregeometry` APRES `\end{titlepage}`, jamais dedans. Place a
% l'interieur, il restaure les marges pendant que l'environnement construit
% encore sa page : la PREMIERE page suivante herite d'une hauteur de bloc
% fausse, et sa derniere ligne est imprimee A CHEVAL SUR LE BORD DU PAPIER --
% coupee en deux dans le sens de la hauteur. Mesure : la ligne « 2.11 Memoire :
% pile et tas » de la table des matieres, tranchee au milieu des lettres.
% Le defaut ne se voit sur AUCUNE autre page, et TeX n'emet aucun avertissement
% (pas d'`Overfull \vbox`) : le bloc a la bonne taille, c'est sa POSITION qui
% deborde. Il ne se detecte donc qu'en regardant le papier.
\restoregeometry
